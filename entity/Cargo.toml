[package]
name = "entity"
version = "0.1.0"
edition = "2021"

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
serde = "1.0.160"
rocket = { version ="0.5.0-rc.3", features = ["secrets"] }
sqlx = {version = "0.6.3", features = ["time"]}